/*
beforeAll(fn, timeout(by default 5 secondes))
beforeEach(fn, timeout(by default 5 secondes))
*/

beforeAll(() => {
  //connect to database
  //empty testing table 
  //add data for testing 
  //prepare everthing befor the testing  
})

afterAll(() => {
    //clean database
  })